import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sln.finance',
  appName: 'Sri Lakshmi Narasimha Finance',
  webDir: 'dist'
};

export default config;
